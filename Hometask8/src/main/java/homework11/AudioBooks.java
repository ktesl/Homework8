package homework11;

public class AudioBooks extends Material {
    public AudioBooks(String title, String author, Genre genre, boolean checkedOut) {
        super(title, author, genre, checkedOut);
    }
}
